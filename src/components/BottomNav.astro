---
// Bottom navbar removed. The top nav (Nav.astro) now hides on scroll-down and
// reveals on scroll-up. Kept as a no-op component so the existing <BottomNav />
// references across all pages don't need editing.
---
